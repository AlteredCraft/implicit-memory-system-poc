# Memory System v2 - Environment Configuration

# NOTE: For single-user POC, API keys are stored in browser localStorage
# These server-side environment variables are OPTIONAL

# Anthropic API Configuration (Optional)
# The web UI will prompt for API key on first use and store in localStorage
# Uncomment to set a server-side default:
# ANTHROPIC_API_KEY=sk-ant-...

# Model selection (Optional, defaults to claude-sonnet-4-5-20250929)
# ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Logging Configuration (Optional)
# APP_LOG_LEVEL=INFO                   # DEBUG|INFO|WARNING|ERROR
# DEPENDENCIES_LOG_LEVEL=WARNING       # Control library verbosity
