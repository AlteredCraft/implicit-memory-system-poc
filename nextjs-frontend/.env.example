# Backend API URL (FastAPI server)
# Used by Next.js API routes to proxy requests to the FastAPI backend
# This stays server-side (not exposed to browser) for better security
NEXT_PUBLIC_API_BASE_URL=http://localhost:8888
